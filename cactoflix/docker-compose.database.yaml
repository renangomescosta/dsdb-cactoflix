services:
  banco:
    image: mongo:latest
    container_name: ${BANCO_NAME}
    command: ["mongod", "--replSet", "cenAdm", "--bind_ip_all"]
    ports:
      - "27017:27017"
    volumes:
      - banco_data:/data/db

volumes:
  banco_data:
